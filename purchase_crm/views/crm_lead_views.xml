<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="purchase_crm_case_form_view_oppor" model="ir.ui.view">
            <field name="name">purchase.crm.lead.oppor.inherited.crm</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_case_form_view_oppor"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button" type="object"
                            name="action_view_purchase_rfq" icon="fa-pencil-square">
                        <field name="rfq_count" widget="statinfo" string="Solicitud(es)"/>
                    </button>
                    <button class="oe_stat_button" type="object" attrs="{'invisible': [('purchase_order_count', '=', 0)]}"
                            name="action_view_purchase_order" icon="fa-shopping-cart">
                        <div class="o_field_widget o_stat_info">
                            <span class="o_stat_value"><field name="purchase_amount_total" widget="monetary" options="{'currency_field': 'company_currency'}"/></span>
                            <span class="o_stat_text"> Compras</span>
                            <field name="purchase_order_count" invisible="1"/>
                        </div>
                    </button>
                </div>
            </field>
        </record>

</odoo>